--- tools/N64ModernRuntime/ultramodern/src/events.cpp
+++ tools/N64ModernRuntime/ultramodern/src/events.cpp
@@ -166,6 +166,15 @@
         case OS_EVENT_SI:
             events_context.si.msg = msg;
             events_context.si.mq = mq_;
+            break;
+        case OS_EVENT_PI:
+            // Do nothing for PI right now, do_dma handles it
+            break;
+        case 14: // OS_EVENT_VI
+            events_context.vi.get_next_state()->mq = mq_;
+            events_context.vi.get_next_state()->msg = msg;
+            events_context.vi.get_next_state()->retrace_count = 1;
+            break;
     }
 }
