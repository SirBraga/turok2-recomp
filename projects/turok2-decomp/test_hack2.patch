--- tools/N64ModernRuntime/ultramodern/src/events.cpp
+++ tools/N64ModernRuntime/ultramodern/src/events.cpp
@@ -148,6 +148,7 @@
 }
 
 extern "C" void osSetEventMesg(RDRAM_ARG OSEvent event_id, PTR(OSMesgQueue) mq_, OSMesg msg) {
+    printf("osSetEventMesg called with event %d, mq %08X\n", event_id, mq_);
     std::lock_guard lock{ events_context.message_mutex };
 
     switch (event_id) {
